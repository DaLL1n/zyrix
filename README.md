# Zyrix - Cryptocurrency Platform

### [🚀 Посмотреть демо-версию](https://dall1n.github.io/zyrix/)

<p align="center">
  <img src="./public/icons/icon-header-logo.svg" alt="Zyrix logo" width="140" />
  
</p>

---

Современная платформа для отслеживания криптовалют с поиском в реальном времени и аналитикой рынка.

## 📋 О проекте

Zyrix — это веб-приложение для работы с криптовалютами, разработанное с использованием современного стека технологий и архитектурного подхода Feature-Sliced Design (FSD). Проект демонстрирует профессиональные навыки frontend-разработки, включая работу с Next.js 16, React 19, TypeScript, и интеграцию с внешними API.

**Статус:** 🚧 В активной разработке

> Важно: текущая версия без адаптивной вёрстки (desktop-only). Мобильная и планшетная версии в планах.

---

## ✨ Реализованные возможности

### ✅ Готово

- **Главная страница** (desktop-only на текущем этапе)
  - Hero-секция с призывом к действию
  - Ключевые метрики рынка криптовалют
  - Обзор основных функций платформы
  - Трендовые криптовалюты с графиками (Recharts)
- **Поиск криптовалют**
  - Модальное окно с поиском в реальном времени
  - Debounce для оптимизации запросов
  - Интеграция с CoinGecko API
  - Отображение результатов с иконками монет
  - Обработка состояний загрузки и ошибок

- **Навигация и UI**
  - Адаптивный Header с навигационным меню
  - Footer с социальными ссылками
  - Переиспользуемые UI-компоненты (Input, Loader, Icon, Logo)

### 🚧 В разработке

- **Система аутентификации**
  - Страница логина/регистрации
  - Форма с переключением Email/Phone
  - Интеграция с социальными сетями (Google)

### 📅 Запланировано

- Детальная страница криптовалюты
- Личный кабинет пользователя
- Избранные монеты
- Портфолио криптовалют

---

## 🗺️ Дальнейшие планы (по макету)

- **Market (/market):** таблица монет с сортировкой, фильтрами, пагинацией; сохранение параметров в query.
- **Spot (/spot):** каталог монет и глубина `/spot/[slug]` с графиком (Recharts), KPI и краткой сводкой.
- **Coin Detail:** расширенная карточка монеты: описание, ссылки, похожие монеты, история цен.
- **Auth (Login/Sign‑Up):** экраны входа/регистрации по макету, валидация, состояния, без бэкенда.
- **Blog (/blog):** список статей и `/blog/[slug]` на MDX, карточки, пагинация, SEO‑мета.
- **Support (/support):** FAQ‑аккордеоны, форма контактов (без отправки), справочные ссылки.
- **Header/Footer:** финализация состояний навигации (hover/active), выпадающие меню, соответствие макету.
- **Заглушки и навигация:** создать пустые страницы для всех пунктов меню + корректная 404.
- **Пути к ассетам:** стандартизировать относительные пути к `icons.svg`, `coin-icons.svg`, изображениям для GitHub Pages (basePath).
- **SEO:** пер‑страничные `title/description/OG`, `sitemap.xml`, `robots.txt`, JSON‑LD (Organization/Article).
- **i18n каркас:** структура локализаций в `shared/lib/i18n`, вынос текстов, переключатель языка.
- **Состояния UI:** скелетоны/empty/error для списков и графиков, единый Loader/Empty/Retry‑паттерн.
- **Тесты/мониторинг:** smoke‑тесты (Playwright по `out/`), unit‑тесты, подключение web‑vitals/analytics.
- **CI/CD:** кэш npm в Actions, контроль размера бандла, публикация превью‑билда артефактом.
- **Адаптив (позже):** внедрение брейкпоинтов `xs/sm/md/lg/xl` после завершения десктопной версии.

---

## 🛠️ Стек технологий

- **Next.js 16** (App Router)
- **React 19**
- **TypeScript 5**
- **SASS/SCSS Modules** (методология BEM)
- **Библиотеки:** TanStack Query v5, Recharts, Zod v4, clsx

---

## 🏛️ Архитектура проекта (Feature-Sliced Design)

<details>
<summary>Нажмите, чтобы развернуть детальное описание структуры проекта</summary>

### Оглавление

- [Общая структура](#общая-структура)
- [Слои архитектуры](#слои-архитектуры)
  - [`app`](#app)
  - [`pages`](#pages)
  - [`widgets`](#widgets)
  - [`features`](#features)
  - [`entities`](#entities)
  - [`shared`](#shared)

---

### Общая структура

```
src/
├── app/             # Инициализация приложения (стили, провайдеры)
├── pages/           # Композиция страниц
├── widgets/         # Композитные блоки UI
├── features/        # Интерактивные функции
├── entities/        # Бизнес-сущности
└── shared/          # Переиспользуемый код
```

---

### Слои архитектуры

#### `app`

Слой инициализации приложения.

| Директория/Файл               | Описание                                                  |
| :---------------------------- | :-------------------------------------------------------- |
| `styles/globals.scss`         | Глобальные стили приложения                               |
| `providers/QueryProvider.tsx` | Провайдер React Query для управления серверным состоянием |

#### `pages`

Композиция страниц из виджетов и фич.

| Страница   | Компоненты                                          | Описание                               |
| :--------- | :-------------------------------------------------- | :------------------------------------- |
| `Home`     | HeroHome, KeyMetrics, FeaturesOverview, MarketTrend | Главная страница с обзором функционала |
| `Auth`     | AuthForm                                            | Страница аутентификации (в разработке) |
| `NotFound` | -                                                   | Страница 404                           |

#### `widgets`

Композитные блоки, собирающие features и entities.

| Виджет             | Описание                                       |
| :----------------- | :--------------------------------------------- |
| `Header`           | Шапка сайта с навигацией и поиском             |
| `Footer`           | Подвал с навигацией и социальными ссылками     |
| `HeroHome`         | Hero-секция главной страницы                   |
| `KeyMetrics`       | Блок с ключевыми метриками рынка               |
| `FeaturesOverview` | Обзор основных возможностей платформы          |
| `MarketTrend`      | Виджет с трендовыми криптовалютами и графиками |

#### `features`

Интерактивные функции для взаимодействия пользователя.

| Feature       | Содержимое  | Описание                                   |
| :------------ | :---------- | :----------------------------------------- |
| `coin-search` | ui/, model/ | Поиск криптовалют с debounce и React Query |
| `auth`        | ui/         | Форма аутентификации (в разработке)        |

**Структура `coin-search`:**

```
coin-search/
├── ui/
│   ├── CoinSearchModal/      # Модальное окно поиска
│   ├── SearchButton/          # Кнопка открытия поиска
│   └── SearchInput/           # Инпут для ввода запроса
├── model/
│   └── useSearchCoins.ts      # Хук с debounce и логикой поиска
└── index.ts
```

#### `entities`

Бизнес-сущности приложения.

| Entity | API                                                    | Model                            | UI       | Описание                     |
| :----- | :----------------------------------------------------- | :------------------------------- | :------- | :--------------------------- |
| `coin` | fetchTrendCoins, fetchSearchCoins, fetchTopSearchCoins | schemas (Zod), normalizeCoinData | CoinList | Работа с данными криптовалют |

**Структура `coin`:**

```
coin/
├── api/                       # Запросы к CoinGecko API
│   ├── fetchTrendCoins.ts
│   ├── fetchSearchCoins.ts
│   └── fetchTopSearchCoins.ts
├── model/
│   ├── schemas.ts             # Zod-схемы для валидации
│   └── normalizeCoinData.ts   # Нормализация данных API
├── lib/
│   └── helpers.ts             # Вспомогательные функции
└── ui/
    └── CoinList/              # Компонент списка монет
```

#### `shared`

Переиспользуемые компоненты, утилиты и конфигурация.

| Категория | Содержимое                                               | Описание                  |
| :-------- | :------------------------------------------------------- | :------------------------ |
| `ui/`     | Input, Icon, Loader, Logo, CoinIcon, NavMenu, SocialList | UI-компоненты             |
| `api/`    | fetchCoin, handleApiResponse                             | API-клиенты и обработчики |
| `lib/`    | formatCurrency, i18n                                     | Утилиты                   |
| `config/` | paths.config.ts                                          | Конфигурация маршрутов    |

**UI-компоненты:**

```
shared/ui/
├── Input/                     # Переиспользуемый инпут
├── Icon/                      # SVG-иконки
├── Loader/                    # Индикатор загрузки
├── Logo/                      # Логотип приложения
├── CoinIcon/                  # Иконка криптовалюты
├── NavMenu/                   # Навигационное меню
└── SocialList/                # Список социальных ссылок
```

---

### Ключевые принципы FSD

1. **Изолированность слоёв** - верхние слои могут использовать нижние, но не наоборот
2. **Публичный API** - каждый модуль экспортирует через `index.ts`
3. **Модульность** - код разбит на независимые модули с чёткой ответственностью

</details>

---

## 🚀 Локальный запуск

1. Клонируйте репозиторий:

   ```bash
   git clone https://github.com/DaLL1n/zyrix.git
   ```

2. Установите зависимости:

   ```bash
   cd zyrix
   npm install
   ```

3. Запустите проект в режиме разработки:

   ```bash
   npm run dev
   ```

4. Откройте [http://localhost:3000](http://localhost:3000) в браузере.

### Другие команды

```bash
npm run build  # Сборка для production
npm run start  # Запуск production-сборки
npm run lint   # Проверка кода
npm run cc     # Создание нового компонента (custom script)
```

---

---

## 🎨 Особенности реализации

### Оптимизация производительности

- **React.memo** для предотвращения лишних ре-рендеров компонентов
- **useCallback/useMemo** для оптимизации функций и вычислений
- **React Query** кэширование с автоматической инвалидацией данных
- **Next.js Image** компонент с оптимизацией изображений

### Работа с данными

- **Zod** схемы для валидации API-ответов от CoinGecko
- **Нормализация данных** перед использованием в компонентах
- **Обработка ошибок** с fallback UI и информативными сообщениями
- **Loading states** для улучшения пользовательского опыта
- **TypeScript** для полной типизации данных

### Архитектурные решения

- **FSD** методология для масштабируемости и поддерживаемости
- **Публичный API** модулей через index.ts
- **Изоляция слоёв** - строгие правила импортов между слоями
- **SCSS Modules** для изоляции стилей компонентов
- **Custom hooks** для переиспользования логики

### Интеграции

**CoinGecko API:**

- Получение трендовых криптовалют (топ-7 по популярности)
- Поиск монет по имени/символу/тикеру
- Загрузка иконок криптовалют с оптимизацией
- Обработка rate limits и ошибок API

---

## 🤝 Контакты

**GitHub:** [DaLL1n](https://github.com/DaLL1n)

**Проект:** [zyrix](https://github.com/DaLL1n/zyrix)

---

## 📝 Примечания для резюме

Этот проект демонстрирует:

- ✅ Владение современным React/Next.js стеком
- ✅ Понимание архитектурных паттернов (FSD)
- ✅ Навыки оптимизации производительности
- ✅ Работа с TypeScript и валидацией данных
- ✅ Интеграция с внешними API
- ❗ Без адаптивной вёрстки в текущей версии (desktop-only)
- ✅ Code quality и best practices

**Статус разработки:** Активная разработка с регулярными коммитами

**Технический уровень:** Middle/Senior Frontend Developer
